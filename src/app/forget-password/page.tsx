import { Flex } from "@chakra-ui/react"

const ForgetPassword = () => {
    return(
        <Flex justifyContent={'center'} alignItems={'center'} bg={"#FAFAFC"}>
            <Flex borderRadius={'lg'} boxShadow={'lg'}>
                
            </Flex>
        </Flex>
    )
}

export default ForgetPassword